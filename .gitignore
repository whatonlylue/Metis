# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
build/
dist/

# Metis project artifacts (data, weights, results are large/generated)
# Anchored to repo root so it doesn't match the src/metis/projects/ package,
# whose exclusion silently broke the packaged wheel.
/projects/
# Local METIS_HOME used during development (see CONTRIBUTING.md)
.metis-dev/
.metis/

# Secrets
.env
*.key

# OS / editor
.DS_Store
.idea/
.vscode/
